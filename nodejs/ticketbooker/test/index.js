'use strict';

var assert = require('assert');
//var ticketbooker = require('../lib');

describe('ticketbooker', function () {
  it('should have unit test!', function () {
    assert(true, true);
  });
});
